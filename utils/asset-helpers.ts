/**
 * Asset helper utilities
 * Re-exports from story-config for convenience
 */

export {
  formatSceneIndex,
  getKeyframeUrl,
  getTransitionUrl,
  getNarrationUrl,
  getSceneAssets,
  R2_BASE_URL,
} from "@/lib/story-config";

